# iSERV Test Generation Pipeline — Python dependencies
# Install: pip install -r testGen/requirements.txt
# Then:    playwright install chromium

# ── Playwright browser automation ─────────────────────────────────────────────
playwright>=1.44.0

# ── pytest runner (execute.py spawns pytest-playwright internally) ─────────────
pytest>=8.0.0
pytest-playwright>=0.5.0

# ── LLM — scenario generation (generate.py) ───────────────────────────────────
openai>=1.30.0

# ── HTTP requests — API auth + multi-session tests (execute.py) ───────────────
requests>=2.31.0

# ── Environment variable loading ───────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Excel report generation (report.py) ───────────────────────────────────────
openpyxl>=3.1.0
